# Backend tests

The booking and payment regression suite should cover ownership, lifecycle, capacity, and idempotent-payment cases before release. Install `pytest` in the backend environment and run `pytest tests`.
